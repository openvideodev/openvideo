{
  "name": "combo",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "fmt": "oxfmt",
    "fmt:check": "oxfmt --check",
    "check-types": "turbo run check-types",
    "changeset": "changeset",
    "version": "changeset version",
    "release": "turbo run build --filter=\"@openvideo/*\" && changeset publish",
    "build:packages": "turbo run build --filter='@openvideo/*'",
    "build:editor-packages": "turbo run build --filter='@openvideo/timeline' --filter='@openvideo/engine-pixi' --filter='@openvideo/core'",
    "publish:editor-packages": "pnpm build:editor-packages && changeset publish"
  },
  "devDependencies": {
    "@changesets/changelog-github": "^0.7.0",
    "@changesets/cli": "^2.31.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "oxfmt": "^0.51.0",
    "oxlint": "^0.16.8",
    "turbo": "^2.5.8",
    "typescript": "5.9.2"
  },
  "lint-staged": {
    "*.{js,ts,tsx,mjs,cjs}": [
      "oxlint",
      "oxfmt"
    ]
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "pnpm@9.15.4",
  "pnpm": {
    "overrides": {
      "@types/react": "19.2.8",
      "@types/react-dom": "19.2.3",
      "canvas": "3.2.3"
    }
  }
}